<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Barking.NoActionBar">
        <activity android:name=".ResDetails"></activity>
        <activity android:name=".QRActivity" />
        <activity android:name=".MyResActivity" />
        <activity
            android:name=".ConfirmationActivity"
            android:label="@string/title_activity_confirmation"
            android:theme="@style/Theme.Barking.NoActionBar" />
        <activity
            android:name=".ReservationActivity"
            android:label="Make Your Reservation" />
        <activity
            android:name=".ParkingsActivity"
            android:label="Select A Parking" />
        <activity
            android:name=".HomeActivity"
            android:label="Select A City" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Barking.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>